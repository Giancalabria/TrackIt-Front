package com.trackit.core

object AppConfig {
    /** Default geocoder bias (Buenos Aires) when no user location is available. */
    const val DEFAULT_GEOCODER_LAT = -34.6037
    const val DEFAULT_GEOCODER_LON = -58.3816
}
